/**function FahtoCelsius (Fahrenheit) {
  return (Fahrenheit-32) / 1.8;
  }

console.log(FahtoCelsius(45));


var text = "La temperatura es de" + FahtoCelsius(45) + "grados Celsius." **/

function calculate(a,b) {
  return a / b;
}

console.log(calculate(5,5));
